apps:
  - name: fake-sensor
    selects:
      - alloc
      - hwrng
    env:
      global:
        # Under most of the observed heap sizes on the NRF52840DK.
        # Only relevant for the ESP32 chips which allocate an empty heap by default.
        heapsize_required:
          - "100000"
        RUSTFLAGS:
          - --cfg pulley_disable_interp_simd